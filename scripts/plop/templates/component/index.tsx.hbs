import { Container } from './styles';
import type { Props } from './types';

export default function {{ properCase name }}({ children }: Props): JSX.Element {
  return <Container>{children}</Container>;
}
